<<%= (is_lowercase) ? name.downcase : name %>
<%if model.xmlId%> id="<%= model.xmlId%>"<%end%>
>
    <%== render :template => 'element', :locals => {model: model} %>
    <%- if !model.comment().nil? -%><comment value="<%= model.comment() %>"/><%- end -%>
    <%- if !model.link().nil? -%><%== render :template => 'resourcereference', :locals => {name: 'link', model: model.link()} %><%- end -%>
</<%= (is_lowercase) ? name.downcase : name %>>
