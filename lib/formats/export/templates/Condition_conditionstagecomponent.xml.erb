<<%= (is_lowercase) ? name.downcase : name %>
<%if model.xmlId%> id="<%= model.xmlId%>"<%end%>
>
    <%== render :template => 'element', :locals => {model: model} %>
    <%- if !model.summary().nil? -%><%== render :template => 'CodeableConcept', :locals => {name: 'summary', model: model.summary()} %><%- end -%>
    <%- if (!model.assessment().nil? && !model.assessment.empty?) -%><%- model.assessment().each do |element| -%><%== render :template => 'resourcereference', :locals => {name: 'assessment', model: element} %><%- end -%><%- end -%>
</<%= (is_lowercase) ? name.downcase : name %>>
