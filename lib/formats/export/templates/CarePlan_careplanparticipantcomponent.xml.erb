<<%= (is_lowercase) ? name.downcase : name %>
<%if model.xmlId%> id="<%= model.xmlId%>"<%end%>
>
    <%== render :template => 'element', :locals => {model: model} %>
    <%- if !model.role().nil? -%><%== render :template => 'CodeableConcept', :locals => {name: 'role', model: model.role()} %><%- end -%>
    <%- if !model.member().nil? -%><%== render :template => 'resourcereference', :locals => {name: 'member', model: model.member()} %><%- end -%>
</<%= (is_lowercase) ? name.downcase : name %>>
