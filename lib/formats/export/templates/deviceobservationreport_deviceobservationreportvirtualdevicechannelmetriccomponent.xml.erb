<<%= (is_lowercase) ? name.downcase : name %>
<%if model.xmlId%> id="<%= model.xmlId%>"<%end%>
>
    <%== render :template => 'element', :locals => {model: model} %>
    <%- if !model.observation().nil? -%><%== render :template => 'resourcereference', :locals => {name: 'observation', model: model.observation()} %><%- end -%>
</<%= (is_lowercase) ? name.downcase : name %>>
