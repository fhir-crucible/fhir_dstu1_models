<<%= (is_lowercase) ? name.downcase : name %>
<%if model.xmlId%> id="<%= model.xmlId%>"<%end%>
>
    <%== render :template => 'element', :locals => {model: model} %>
    <%- if !model.code().nil? -%><%== render :template => 'codeableconcept', :locals => {name: 'code', model: model.code()} %><%- end -%>
    <%- if (!model.channel().nil? && !model.channel.empty?) -%><%- model.channel().each do |element| -%><%== render :template => 'deviceobservationreport_deviceobservationreportvirtualdevicechannelcomponent', :locals => {name: 'channel', model: element} %><%- end -%><%- end -%>
</<%= (is_lowercase) ? name.downcase : name %>>
